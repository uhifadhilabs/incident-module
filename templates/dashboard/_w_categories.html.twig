{# IN·09 — THE REFERENCE CARD: all four kinds of incident spelled out, with the
   sub-categories under each and which department leads it. The ONE place the
   list is written out in full.

   "Leads" is ORDERING, NOT ACCESS. Every department can open every category; the
   card says so, because a label nobody explains is a label people assume the
   worst about. #}
<div class="c" data-w="categories">
    <span class="tab">What the categories are<span class="src">· the reference card · {{ dashboard.categories|length }} kinds</span></span>
    <div class="i-tax">
        {% for category in dashboard.categories %}
            <div class="i-taxg{{ category.slug in dashboard.filter.categorySlugs ? ' on' }}">
                <a class="r" href="{{ path('incident_dashboard', {uuid: area.uuid}|merge(dashboard.filter.onlyCategory(category.slug).toQuery)) }}">
                    <i class="i-hue-{{ category.colourKey }}"></i><b>{{ category.label }}</b><span class="n">{{ dashboard.categoryCount(category) }}</span>
                </a>
                <span class="lens">leads: {{ category.leadsLine }}</span>
                <span class="i-taxrun">{% for subcategory in category.subcategories %}<span>{{ subcategory.label }}</span>{% endfor %}</span>
            </div>
        {% endfor %}
    </div>
    <div class="use i-taxnote">“Leads” is ordering, not access — every department can open every category. This card is the only place the list is written out; the counts live next door.</div>
</div>
