<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true">
    <php>
        <env name="KERNEL_CLASS" value="Uhifadhi\Incident\Tests\Integration\TestKernel"/>
        <!--
            A REAL PostGIS database. The integration and functional suites rebuild
            the schema per test and assert on what was actually stored — the module
            keeps points and asks PostGIS which zone one falls in, and a suite that
            only ever saw a mock would prove nothing about either.

            Locally this is the shared fundi-postgis-test container; CI runs its own
            service and overrides this with the same URL on port 5432.
        -->
        <env name="INCIDENTS_TEST_DATABASE_URL"
             value="postgresql://app:app@127.0.0.1:5434/incidents_bundle_test?serverVersion=17&amp;charset=utf8"/>
    </php>
    <testsuites>
        <testsuite name="default">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source restrictNotices="true" restrictWarnings="true">
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>